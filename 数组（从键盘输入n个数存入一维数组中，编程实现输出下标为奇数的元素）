#include<stdio.h>
int main(){
	int n,a[100],i;
	scanf("%d",&n);
	for(i=0;i<n;i++)
		scanf("%d",&a[i]);
	for(i=1;i<n;i+=2){
		if(i>1)
			printf(",%d",a[i]);
		else 
            printf("%d",a[i]);
	}
	return 0;
}
