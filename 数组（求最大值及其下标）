#include<stdio.h>
int main(){
	int a[10],n,i,max=0;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	  scanf("%d",&a[i]);
	for(i=1;i<n;i++){
		if(a[max]<a[i])
			max=i;
	}
	printf("%d %d",a[max],max);
	return 0;
}
